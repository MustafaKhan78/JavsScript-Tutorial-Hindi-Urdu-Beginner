// que => Create a function that takes a string and returns it as an integer.
function stringInt(str) {
     // parseInt(str);
    //parseFloat(str)
    //Number(str)
    return +str

}


console.log(stringInt("6"));

console.log(stringInt("1000"));

console.log(stringInt("12"));